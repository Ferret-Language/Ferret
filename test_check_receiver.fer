import "std/io";

type Point struct { .X: i32, .Y: i32 };

fn (p: &Point) getX() -> i32 {
    return p.X;
}

fn (p: &mut Point) move(dx: i32, dy: i32) {
    p.X += dx;
    p.Y += dy;
}

fn main() {
    let point: Point = { .X = 0, .Y = 0 };
    point.move(5, 10);
}