

import "std/testing"

fn main() -> void {
    print("app")
}

test "smoke" {
    testing::ExpectEq(2 + 2, 4, "math broke")
}

test "try multiply" {
    testing::Expect(2 * 10 == 20, "Condition failed")
}

