import "std/io";

type Counter struct {
    .Value: i32
};

fn main() {
    let c1 := { .Value = 42 } as Counter;
    io::Println(c1.Value);  // Should print 42
    
    let c2 := c1;  // Copy c1 to c2
    io::Println(c2.Value);  // Should also print 42
}
