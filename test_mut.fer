fn id(p: &i32) -> &i32 { return p; }

fn main() {
    let a := 10;
    let r := id(&a);   // r now holds a borrow

    let w := &mut a;   // should error if r is still alive
    *w = 20;

    let n := r;
}