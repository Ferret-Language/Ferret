import "std/string"
import "std/io"

fn main() -> void {
    let mut buf = string::New()
    buf.Write("Hello")
    buf.Write(", world")
    println(buf.AsStr())

    let mut newBuf = string::New()
    
    io::Write(newBuf, buf.AsStr())

    println(newBuf.AsStr())

    buf.Release()
}
