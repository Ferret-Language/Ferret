fn main() -> void {
    let mut values = map[str]i32{"one" => 1}
    println(to_str(values["one"]))
    values["one"] = 2
    println(to_str(values["one"]))

    let mut words = map[str]str{"a" => "alpha"}
    println(words["a"])
    words["a"] = "beta"
    println(words["a"])
}
