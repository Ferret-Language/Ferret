fn main() -> void {
    let mut values = map[str]i32{"one" => 1}
    println(values["one"] as str)
    values["one"] = 2
    println(values["one"] as str)

    let mut words = map[str]str{"a" => "alpha"}
    println(words["a"])
    words["a"] = "beta"
    println(words["a"])
}
