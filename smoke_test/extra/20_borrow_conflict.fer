import "std/io";

fn main() {
    let a := 10;
    let r: &i32 = &a;
    io::Println(r);
    let m: &mut i32 = &mut a; // expected error: mutable + immutable borrow
    *m = 12;
    io::Println(a);
}
