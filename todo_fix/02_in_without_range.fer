import "std/io";
// TODO: should be supported (in operator for arrays)
// Current error: 'in' operator requires a range expression
fn main() {
    let x: i32 = 2;
    let arr: []i32 = [1, 2, 3];
    if x in arr {
        io::Println(x);
    }
}
