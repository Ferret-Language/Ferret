import "std/io";

fn test(x: i32) {
    // Should NOT warn: x is a parameter, not constant
    if x in 5..20 {
        io::Println("x is in range");
    } else {
        io::Println("x is not in range");
    }
}

fn main() {
    test(10);
}
