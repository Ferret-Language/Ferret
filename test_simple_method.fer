import "std/io";

type Counter struct {
    .Value: i32
};

fn (c: Counter) printValue() {
    io::Println(c.Value);
}

fn main() {
    let c := { .Value = 42 } as Counter;
    io::Println(c.Value);  // Direct access - should print 42
    c.printValue();  // Method call - should also print 42
}
