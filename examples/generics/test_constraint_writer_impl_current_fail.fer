// EXPECT: FAIL (i32 does not satisfy writer)
constraint writer = interface {
    Write(buf: []byte) -> i32
};

fn useWriter<T: writer>(w: T) -> i32 {
    return 0;
}

fn main() -> i32 {
    return useWriter(10);
}
