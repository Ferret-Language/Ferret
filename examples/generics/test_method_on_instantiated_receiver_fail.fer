// EXPECT: PASS (method on instantiated generic receiver)
type Box<T> struct {
    .Value: T
};

fn (b: Box<i32>) twice() -> i32 {
    return b.Value * 2;
}

fn main() -> i32 {
    let b := { .Value = 9 } as Box<i32>;
    return b.twice();
}
