type Node struct {
    Child *Node
    Value i32 = 0
}

fn rebuild(n Node, replacement *Node) i32 {
    let mut current = n
    let child = current.Child
    current.Child = replacement
    let again = current
    return again.Value
}
