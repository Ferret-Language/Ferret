fn main() i32 {
    let s = "hello"
    let bytes = s as []u8
    let chars = s as []char
    let s2 = bytes as str
    let s3 = chars as str
    let rp = str_data(&s)
    let typed = unsafe (rp as *u8)
    let first = *typed
    return (str_len(&s2) + str_len(&s3) + (first as usize)) as i32
}
