import "math/vec2"
import "util/build"
import "std/math"

fn run() i32 {
    let mut x: i32 = 0
    while x < 10 {
        x = x + 1
        if x == 5 {
            break
        }
        continue
    }
    return x
}

type Point struct {
    X i32
    Y i32
}

fn (p Point) nothing(comptime a i32) {
    
}

fn main() i32 {
    let num1 = 1023554363
    let num2 : i8 = num1 as i8
    let p = build::Origin()
    if p == .{ .X = 0, .Y = 0 } {
        return vec2::Len2(copy p)
    }
    
    let point: Point = .{ .X = 1, .Y = 2 }
    point.nothing(23)

    let boolean = true
    //num2 = boolean
    let val = math::ClampToZeros(-34);
    
    return 1
}
