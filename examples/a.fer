import "std/io";

type D struct { .X: i32 };
type D2 struct{ .X: i32 };

fn (d: D) dummy() {
    
}

type ann interface{
    dummy()
};
type any2 interface{
    dummy()
};

type Int i32;
type Int2 i32;

fn main() {

    let ii: Int = 10;
    //let jj: Int2 = ii;
    
    let anyval : D2 = { .X = 20 };
    let another : any2 = anyval;

    let a := #10;
    let addr_a := addr(&a);
    let haddr_a := heap_addr(&a);
    
    let b := @a;
    //io::Println(a);  
    let addr_b := addr(&b);
    let haddr_b := heap_addr(&b);

    a = 100;
    
    let naddr_a := addr(&a);
    let nhaddr_a := heap_addr(&a);

    io::Println("Address of a:", addr_a);
    io::Println("Address of b:", addr_b);
    io::Println("H Address of a:", haddr_a);
    io::Println("H Address of b:", haddr_b);
    io::Println("New Address of a:", naddr_a);
    io::Println("New H Address of a:", nhaddr_a);

    io::Println(a, b);

    //io::Println(len(a));
}