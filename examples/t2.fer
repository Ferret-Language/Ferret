import "std/io";

fn main() {
    let name: str = "Ferret";
    let version: i32 = 1;
    
    let m := {
        {.X = 0} => ".x",
        {.X = 1} => ".x .y",
    };

    let val : str = m[{.X = 0}];

    io::Println(val);
}