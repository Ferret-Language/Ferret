import "std/io";

fn main() {
    let scores := {
        "alpha" => 1,
        "beta" => 2
    };

    io::Println(scores["alpha"]);
    io::Println(scores["gamma"]);

    scores["gamma"] = 3;
    io::Println(scores["gamma"]);
}
