import "std/io";
// With unit compilation, lib.fer is in the same folder,
// so we can access its symbols directly without importing!

fn main() {
    let c := NewCounter();  // Direct access - no lib:: prefix needed
    c.Inc();
    c.Inc();
    io::Println(c.Get());
}
