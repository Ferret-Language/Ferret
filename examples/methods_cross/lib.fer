type Counter struct {
    .Value: i32
};

fn (c: &mut Counter) Inc() {  // Changed to &mut
    c.Value++;
}

fn (c: Counter) Get() -> i32 {
    return c.Value;
}

fn NewCounter() -> Counter {
    return { .Value = 0 } as Counter;
}
