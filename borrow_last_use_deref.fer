import "std/io";

fn main() {
    let a := 10;
    let b: &mut i32 = &mut a;
    io::Println(*b); // last use of b
    let c: &mut i32 = &mut a; // ok: b released after last use
    *c = 11;
    io::Println(a); // expected output: 10 then 11
}
