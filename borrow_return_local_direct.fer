import "std/io";

fn MakeRef() -> &i32 {
    let a := 10;
    return &a; // allowed: direct borrow of local is lowered to out-param move
}

fn main() {
    let r: &i32 = MakeRef();
    io::Println(*r); // expected output: 10
}
