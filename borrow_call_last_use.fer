import "std/io";

fn Inc(x: &mut i32) {
    *x = *x + 1;
    io::Println(*x);
}

fn main() {
    let a := 1;
    let b: &mut i32 = &mut a;
    Inc(b); // last use of b
    let c: &mut i32 = &mut a; // ok: b released after call
    *c = 5;
    io::Println(a); // expected output: 2 then 5
}
