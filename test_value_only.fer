import "std/io";

type Counter struct {
    .Value: i32
};

fn (c: Counter) getValue() -> i32 {
    return c.Value;
}

fn main() {
    let c := { .Value = 42 } as Counter;
    let result := c.getValue();
    io::Println(result);
}
